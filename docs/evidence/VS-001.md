# VS-001 evidence record

**Slice:** Controller-Ready Hearth Desktop
**Status:** active; not audit-ready
**Evidence date:** 2026-08-21
**Owner verdict:** pending

## Revisions and artifacts

| Item | Exact identity | State |
|---|---|---|
| Repository baseline | `e0b145fc8e6e6127906032b4c0b526456637eb71` | Recorded clean starting point |
| Image-content revision | `ed70d875e132f5bf814da8302dea1740d324f7af` | Exact recipe, session, recovery, and image-verifier implementation |
| Contract-suite revision | `685a3c4bdd733d51c003a9533577d915a383b37b` | Exact tests and CI gate used for the recorded local/native pass |
| Candidate build revision | Pending complete image build | Must contain both exact revisions above |
| BlueBuild CLI | `0.9.37`, commit `513065f9`; amd64 installer manifest `sha256:7d881ae7764cd3afeb3337066a596f262bf0691192a23d5b6766b07c044378d1`; binary `sha256:832b5e8a03ebbd54af42c9cc88124c8a7619d51177fa0e2cb3eb8633f8894de3` | Every installer layer verified before extraction |
| Generated Containerfile | `sha256:6337f4afccb96e42f6b855189a0f67cff1ec17a62637684be64ef58091ebbfe6` | Generated on x86_64; exact CLI reference is `v0.9.37-installer` |
| DMS source inspected for contracts | `2f4afeb8892c6c2b5a3519771e465af499762a2f` | Recorded upstream reference; installed RPM version pending |
| Bazzite target contract | Bazzite 44; `steamos-manager 26.4.1`; `steamos-manager-powerstation-0~20260819.git5438d60-3.fc44.x86_64` | Read-only host inspection |
| Booted target deployment | `44.20260820`; OCI manifest `sha256:148c53b3858688d10ef29b571764fbdf6bbf3b6afeb56c9755096d85e46ef77f`; Bazzite revision `5b70abe3146bd2e4b5144b02a32f710eaa539a20` | Recorded reference, not candidate base proof |
| Bazzite base image | `ghcr.io/ublue-os/bazzite-deck@sha256:148c53b3858688d10ef29b571764fbdf6bbf3b6afeb56c9755096d85e46ef77f` | Resolved by BlueBuild generation for `linux/amd64` |
| niri RPM | Fedora package | NEVRA pending build |
| DMS RPM | `avengemedia/dms` COPR package | NEVRA pending build |
| Signed hearthOS OCI | `ghcr.io/akippnn/starlight-hearth` | Candidate digest pending |

The attached legacy `starlightOS.md` is superseded reference material outside
this repository and was not modified.

## Automated evidence

Command:

```bash
python3 -m unittest discover -s tests -v
```

Result on 2026-08-21: **pass, 16 tests** on both macOS and the x86_64 Bazzite target.

Covered behavior:

- supported Desktop, Gaming Mode, and KDE recovery command sequences;
- Gaming Mode return remains available when the Hearth Desktop session is missing or unadvertised;
- missing tool, missing command, unadvertised session, and invalid invocation fail closed;
- new-user defaults, owner-config/backup preservation, and idempotent bootstrap;
- recipe identity/order, retained Flatpaks/Tailscale/signing, public session and justfile entrypoints;
- parseable Hearth theme, steamos-manager TOML, and controller v1 fixture;
- shell syntax, executable permissions, stale-stub absence, and mutable-`latest` prohibition.

The final image script independently fails a build when hearthOS identity,
compatibility fields, desktop packages, executable/session files, DMS service
wiring, Tailscale enablement, or stale-stub removal do not match the contract.

## BlueBuild expansion evidence

BlueBuild 0.9.37 reported the recipe structure valid and resolved the amd64 base
to the digest above. Expansion exposed an integration defect: setting
`blue-build-tag` to `v0.9.37-installer` produced the nonexistent
`v0.9.37-installer-installer` image reference. The recipe and regression test
now pin `v0.9.37`; corrected expansion emits exactly
`ghcr.io/blue-build/cli:v0.9.37-installer` and the Containerfile digest above.

After the correction, deterministic expansion succeeded with cached structural
evidence. Repeated attempts to refresh every remote module schema were
intermittently interrupted by DNS/timeouts at `schema.blue-build.org`; this did
not change the generated artifact and remains external transport evidence, not
a completed image-build result.

## Upstream contract evidence

- DMS Fedora packaging and niri setup were inspected at the DMS revision above.
- DMS documents `dms.service`, custom theme settings, and IPC actions used by the Hearth bindings.
- Pinned DMS source review showed that bare `dms setup` prompts interactively and may request a privileged input-group change. It was removed from automatic session startup; Hearth supplies the minimal niri integration directly.
- niri remains the Fedora package and starts through `niri-session`; configuration is validated by the candidate gate.
- The target's `steamosctl 26.4.1` exposes every command used for desktop-session selection and Gaming/Desktop transitions. Its live session list format is covered by the adapter parser.
- The target session-manager log identifies `/usr/share/steamos-manager/user.d/config.toml` as its packaged configuration drop-in. An initial `/etc/steamos-manager/config.toml` assumption was rejected, converted into a regression assertion, and replaced before image build.

## Pending integrated evidence

- complete image build;
- signed candidate and immutable candidate OCI digest;
- installed niri/DMS package NEVRAs;
- built `os-release`, session files, user-service wants link, executable modes, and justfile validation;
- `niri validate`, live DMS service/IPC, and offline Desktop startup;
- compatibility matrix and five repeated Gaming↔Desktop cycles;
- DMS failure, KDE, TTY, and previous-deployment recovery.

## Target-host access note

Passwordless access to the owner-provided x86_64 host `aki@bazzite` was restored
on 2026-08-21. Read-only target contract inspection and isolated build-workspace
operations are available. No deployment or live session state has been changed;
the physical controller audit remains pending a built candidate.

The booted Bazzite image is present in `/sysroot/ostree/repo`, but an
unprivileged `ostree container image reexport` was denied on a root-owned layer.
No Podman image, deployment, or session state was changed. A complete local
build therefore needs an owner-authorized root export of the cached base, or the
repository's build workflow after pushing with Git over SSH and confirming its
existing signing secret is available.

## Owner audit record

| Check | Result | Notes |
|---|---|---|
| Rebase by signed OCI digest and reboot | Pending | |
| Gaming Mode, TTY, prior deployment | Pending | |
| Hearth Desktop v1 selected | Pending | |
| niri + DMS controller startup | Pending | |
| Compatibility matrix A–C | Pending | |
| Five transitions, one offline | Pending | |
| DMS failure and KDE recovery | Pending | |

Final acceptance is reserved for @akippnn.
